import React, { PureComponent } from 'react';
import { AppRegistry, StyleSheet, Text, TouchableOpacity, View } from 'react-native';


export default class AcceptModal extends PureComponent {
    render() {
        return (
            <View>Something</View> 
        );
    }
}